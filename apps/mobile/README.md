# Muxport mobile

Flutter client shell for Muxport on iOS and Android.

The current screens use demo data while the encrypted connector transport and
state layer are built. Buttons must not be treated as performing real remote or
credential operations until they are connected to tested application services.

```sh
flutter pub get
flutter analyze
flutter test
flutter run
```
