FROM rust:1.85-slim as builder
WORKDIR /usr/src/muxport
RUN apt-get update && apt-get install -y pkg-config libssl-dev protobuf-compiler
COPY . .
RUN cargo build --release -p relay

FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y ca-certificates libssl3 && rm -rf /var/lib/apt/lists/*
COPY --from=builder /usr/src/muxport/target/release/muxport-relay /usr/local/bin/
EXPOSE 8080
ENTRYPOINT ["/usr/local/bin/muxport-relay"]
